YT Creator Pro Admin Control Suite TOTAL LOCK + STAFF MANAGEMENT

Changed related files only:
- admin/index.html
- assets/js/admin-control-suite.js
- assets/css/cms-control.css
- ADMIN-CONTROL-SUITE-TOTAL-LOCK.sql
- supabase/functions/create-admin-staff/index.ts

Deployment order:
1) Run ADMIN-CONTROL-SUITE-TOTAL-LOCK.sql in Supabase SQL Editor.
2) Deploy the Edge Function: supabase/functions/create-admin-staff/index.ts as `create-admin-staff`.
3) Keep Edge Function JWT verification enabled. It uses the Supabase service role only server-side.
4) Open Admin Dashboard > Control Suite > Roles & Permissions > Create / Invite Staff.

Staff flow:
Super Admin enters name/email/role -> warning/confirmation -> secure Supabase Auth email invite -> admin_users authorization -> role assignment -> staff accepts invite -> logs in -> permissions control what the role can access.

Warnings:
- Never expose SUPABASE_SERVICE_ROLE_KEY in browser code.
- Super Admin has destructive/full-control privileges.
- Audit permanent deletion cannot be restored.
- Revenue delete archives from the Revenue view; payment ledger remains intact.
- Trash permanent delete can cascade through related data; confirmation is required.
