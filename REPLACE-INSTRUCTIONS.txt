ADMIN LOGIN — LEFT PANEL MATCH FIX (CHANGED ONLY)

Replace ONLY:
assets/css/admin-login-multicolor-fix.css

Change:
- Left Admin Login visual panel now uses the EXACT same multi-color gradient as the "Login to Admin" button.
- Right-side login card/form is NOT changed.
- Authentication, routing, Supabase, admin-auth.js, user login, dashboard, sidebar, services and all other working files are NOT changed.
